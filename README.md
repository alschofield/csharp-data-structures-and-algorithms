# Data Structures and Algorithms in C#

The `src/` taxonomy mirrors the canonical C curriculum exactly. Its 27 leaves each carry a contract README; `TEST-SPECS.md` provides generated xUnit targets for every leaf. No production C# source is supplied.

## Verification

`dotnet test` becomes meaningful after test files and user-owned production APIs are introduced. Implement each learning target directly: do not substitute BCL collections, search helpers, or sorting helpers.
